import React from "react";
// import { useTasksCtx } from "../../store/TasksContext";
// import { useSprintsCtx } from "../../store/SprintsContext";

// const taskStatuses = ["To Do", "In Progress", "On Hold", "Done"]

// interface KanbanDatasetStateType {

// }

const KanbanView: React.FC = () => {
    // const [kanbanDataset, setCanbanDataset] = useState<[]>([]) 
    // const {tasks} = useTasksCtx();
    // const {sprints} = useSprintsCtx();
    // console.log(sprints);


    return (
        <>
            <p>kanban</p>
        </>
    )
}

export default KanbanView;